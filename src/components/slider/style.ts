import {StyleSheet} from "react-native";

export const sliderStyles = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        overflow: "visible",
    },
    track: {
        position: "relative",
    },
    thumb: {
        justifyContent: "center",
        alignItems: "center",
        elevation: 7,
    },
});
